build:
  docker:
    web: docker/app/Dockerfile.web
    worker:
      dockerfile: docker/app/Dockerfile.web
      target: builder # builderのイメージをbuildする際に使う
release:
  image: worker # 上記のworkerのイメージをreleaseフェーズでも使う
run:
  web: /main